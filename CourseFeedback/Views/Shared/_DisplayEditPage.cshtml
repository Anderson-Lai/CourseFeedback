@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@{
    var user = ViewBag.User ?? string.Empty;
    var viewModel = ViewBag.Model ?? string.Empty;

    string rerouteBack = "Courses";
    string routeId = "@Model.CourseCode";

    if(viewModel == "Replies")
    {
        rerouteBack = "Comments";
        routeId = "@Model.CommentId";
    }
}

@model dynamic

@* @if (user.Id == Model.UserId || user.Email == "admin@ycdsbk12.ca")
{
    <div>
        <form method="post">
            <div class="d-flex flex-column">
                <label>Edit your comment:</label>
                <textarea asp-for="Text">@Model.Text</textarea>
                <div class="d-flex">
                    <button type="submit" class="btn btn-primary my-3 px-3 fs-5" style="width: fit-content"
                            asp-controller="@rerouteBack" asp-action="Edit" asp-route-id="@Model.Id">
                        Edit
                    </button>
                    <a class="btn btn-danger m-2 my-3 px-3 fs-5" style="width: fit-content"
                       asp-controller="@rerouteBack" asp-action="Delete" asp-route-id="@Model.Id">
                        Delete
                    </a>
                </div>

            </div>
        </form>
    </div>
}
else
{
    <h3>
        You do not have permission to edit this.
        Please use the account that posted this to gain access.
    </h3>
    <a asp-controller="@rerouteBack" asp-action="HandleEditError" asp-route-id="@routeId"
       class="fs-3">
        Go back
    </a>
} *@